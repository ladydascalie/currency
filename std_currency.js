var stdCurrency = {
    // AED currency object
        AED: { code: "AED", minorUnits: 2, factor: 100},
    // AFN currency object
        AFN: { code: "AFN", minorUnits: 2, factor: 100},
    // ALL currency object
        ALL: { code: "ALL", minorUnits: 2, factor: 100},
    // AMD currency object
        AMD: { code: "AMD", minorUnits: 2, factor: 100},
    // ANG currency object
        ANG: { code: "ANG", minorUnits: 2, factor: 100},
    // AOA currency object
        AOA: { code: "AOA", minorUnits: 2, factor: 100},
    // ARS currency object
        ARS: { code: "ARS", minorUnits: 2, factor: 100},
    // AUD currency object
        AUD: { code: "AUD", minorUnits: 2, factor: 100},
    // AWG currency object
        AWG: { code: "AWG", minorUnits: 2, factor: 100},
    // AZN currency object
        AZN: { code: "AZN", minorUnits: 2, factor: 100},
    // BAM currency object
        BAM: { code: "BAM", minorUnits: 2, factor: 100},
    // BBD currency object
        BBD: { code: "BBD", minorUnits: 2, factor: 100},
    // BDT currency object
        BDT: { code: "BDT", minorUnits: 2, factor: 100},
    // BGN currency object
        BGN: { code: "BGN", minorUnits: 2, factor: 100},
    // BHD currency object
        BHD: { code: "BHD", minorUnits: 3, factor: 1000},
    // BIF currency object
        BIF: { code: "BIF", minorUnits: 0, factor: 1},
    // BMD currency object
        BMD: { code: "BMD", minorUnits: 2, factor: 100},
    // BND currency object
        BND: { code: "BND", minorUnits: 2, factor: 100},
    // BOB currency object
        BOB: { code: "BOB", minorUnits: 2, factor: 100},
    // BOV currency object
        BOV: { code: "BOV", minorUnits: 2, factor: 100},
    // BRL currency object
        BRL: { code: "BRL", minorUnits: 2, factor: 100},
    // BSD currency object
        BSD: { code: "BSD", minorUnits: 2, factor: 100},
    // BTN currency object
        BTN: { code: "BTN", minorUnits: 2, factor: 100},
    // BWP currency object
        BWP: { code: "BWP", minorUnits: 2, factor: 100},
    // BYN currency object
        BYN: { code: "BYN", minorUnits: 2, factor: 100},
    // BZD currency object
        BZD: { code: "BZD", minorUnits: 2, factor: 100},
    // CAD currency object
        CAD: { code: "CAD", minorUnits: 2, factor: 100},
    // CDF currency object
        CDF: { code: "CDF", minorUnits: 2, factor: 100},
    // CHE currency object
        CHE: { code: "CHE", minorUnits: 2, factor: 100},
    // CHF currency object
        CHF: { code: "CHF", minorUnits: 2, factor: 100},
    // CHW currency object
        CHW: { code: "CHW", minorUnits: 2, factor: 100},
    // CLF currency object
        CLF: { code: "CLF", minorUnits: 4, factor: 10000},
    // CLP currency object
        CLP: { code: "CLP", minorUnits: 0, factor: 1},
    // CNY currency object
        CNY: { code: "CNY", minorUnits: 2, factor: 100},
    // COP currency object
        COP: { code: "COP", minorUnits: 2, factor: 100},
    // COU currency object
        COU: { code: "COU", minorUnits: 2, factor: 100},
    // CRC currency object
        CRC: { code: "CRC", minorUnits: 2, factor: 100},
    // CUC currency object
        CUC: { code: "CUC", minorUnits: 2, factor: 100},
    // CUP currency object
        CUP: { code: "CUP", minorUnits: 2, factor: 100},
    // CVE currency object
        CVE: { code: "CVE", minorUnits: 2, factor: 100},
    // CZK currency object
        CZK: { code: "CZK", minorUnits: 2, factor: 100},
    // DJF currency object
        DJF: { code: "DJF", minorUnits: 0, factor: 1},
    // DKK currency object
        DKK: { code: "DKK", minorUnits: 2, factor: 100},
    // DOP currency object
        DOP: { code: "DOP", minorUnits: 2, factor: 100},
    // DZD currency object
        DZD: { code: "DZD", minorUnits: 2, factor: 100},
    // EGP currency object
        EGP: { code: "EGP", minorUnits: 2, factor: 100},
    // ERN currency object
        ERN: { code: "ERN", minorUnits: 2, factor: 100},
    // ETB currency object
        ETB: { code: "ETB", minorUnits: 2, factor: 100},
    // EUR currency object
        EUR: { code: "EUR", minorUnits: 2, factor: 100},
    // FJD currency object
        FJD: { code: "FJD", minorUnits: 2, factor: 100},
    // FKP currency object
        FKP: { code: "FKP", minorUnits: 2, factor: 100},
    // GBP currency object
        GBP: { code: "GBP", minorUnits: 2, factor: 100},
    // GEL currency object
        GEL: { code: "GEL", minorUnits: 2, factor: 100},
    // GHS currency object
        GHS: { code: "GHS", minorUnits: 2, factor: 100},
    // GIP currency object
        GIP: { code: "GIP", minorUnits: 2, factor: 100},
    // GMD currency object
        GMD: { code: "GMD", minorUnits: 2, factor: 100},
    // GNF currency object
        GNF: { code: "GNF", minorUnits: 0, factor: 1},
    // GTQ currency object
        GTQ: { code: "GTQ", minorUnits: 2, factor: 100},
    // GYD currency object
        GYD: { code: "GYD", minorUnits: 2, factor: 100},
    // HKD currency object
        HKD: { code: "HKD", minorUnits: 2, factor: 100},
    // HNL currency object
        HNL: { code: "HNL", minorUnits: 2, factor: 100},
    // HRK currency object
        HRK: { code: "HRK", minorUnits: 2, factor: 100},
    // HTG currency object
        HTG: { code: "HTG", minorUnits: 2, factor: 100},
    // HUF currency object
        HUF: { code: "HUF", minorUnits: 2, factor: 100},
    // IDR currency object
        IDR: { code: "IDR", minorUnits: 2, factor: 100},
    // ILS currency object
        ILS: { code: "ILS", minorUnits: 2, factor: 100},
    // INR currency object
        INR: { code: "INR", minorUnits: 2, factor: 100},
    // IQD currency object
        IQD: { code: "IQD", minorUnits: 3, factor: 1000},
    // IRR currency object
        IRR: { code: "IRR", minorUnits: 2, factor: 100},
    // ISK currency object
        ISK: { code: "ISK", minorUnits: 0, factor: 1},
    // JMD currency object
        JMD: { code: "JMD", minorUnits: 2, factor: 100},
    // JOD currency object
        JOD: { code: "JOD", minorUnits: 3, factor: 1000},
    // JPY currency object
        JPY: { code: "JPY", minorUnits: 0, factor: 1},
    // KES currency object
        KES: { code: "KES", minorUnits: 2, factor: 100},
    // KGS currency object
        KGS: { code: "KGS", minorUnits: 2, factor: 100},
    // KHR currency object
        KHR: { code: "KHR", minorUnits: 2, factor: 100},
    // KMF currency object
        KMF: { code: "KMF", minorUnits: 0, factor: 1},
    // KPW currency object
        KPW: { code: "KPW", minorUnits: 2, factor: 100},
    // KRW currency object
        KRW: { code: "KRW", minorUnits: 0, factor: 1},
    // KWD currency object
        KWD: { code: "KWD", minorUnits: 3, factor: 1000},
    // KYD currency object
        KYD: { code: "KYD", minorUnits: 2, factor: 100},
    // KZT currency object
        KZT: { code: "KZT", minorUnits: 2, factor: 100},
    // LAK currency object
        LAK: { code: "LAK", minorUnits: 2, factor: 100},
    // LBP currency object
        LBP: { code: "LBP", minorUnits: 2, factor: 100},
    // LKR currency object
        LKR: { code: "LKR", minorUnits: 2, factor: 100},
    // LRD currency object
        LRD: { code: "LRD", minorUnits: 2, factor: 100},
    // LSL currency object
        LSL: { code: "LSL", minorUnits: 2, factor: 100},
    // LYD currency object
        LYD: { code: "LYD", minorUnits: 3, factor: 1000},
    // MAD currency object
        MAD: { code: "MAD", minorUnits: 2, factor: 100},
    // MDL currency object
        MDL: { code: "MDL", minorUnits: 2, factor: 100},
    // MGA currency object
        MGA: { code: "MGA", minorUnits: 2, factor: 100},
    // MKD currency object
        MKD: { code: "MKD", minorUnits: 2, factor: 100},
    // MMK currency object
        MMK: { code: "MMK", minorUnits: 2, factor: 100},
    // MNT currency object
        MNT: { code: "MNT", minorUnits: 2, factor: 100},
    // MOP currency object
        MOP: { code: "MOP", minorUnits: 2, factor: 100},
    // MRU currency object
        MRU: { code: "MRU", minorUnits: 2, factor: 100},
    // MUR currency object
        MUR: { code: "MUR", minorUnits: 2, factor: 100},
    // MVR currency object
        MVR: { code: "MVR", minorUnits: 2, factor: 100},
    // MWK currency object
        MWK: { code: "MWK", minorUnits: 2, factor: 100},
    // MXN currency object
        MXN: { code: "MXN", minorUnits: 2, factor: 100},
    // MXV currency object
        MXV: { code: "MXV", minorUnits: 2, factor: 100},
    // MYR currency object
        MYR: { code: "MYR", minorUnits: 2, factor: 100},
    // MZN currency object
        MZN: { code: "MZN", minorUnits: 2, factor: 100},
    // NAD currency object
        NAD: { code: "NAD", minorUnits: 2, factor: 100},
    // NGN currency object
        NGN: { code: "NGN", minorUnits: 2, factor: 100},
    // NIO currency object
        NIO: { code: "NIO", minorUnits: 2, factor: 100},
    // NOK currency object
        NOK: { code: "NOK", minorUnits: 2, factor: 100},
    // NPR currency object
        NPR: { code: "NPR", minorUnits: 2, factor: 100},
    // NZD currency object
        NZD: { code: "NZD", minorUnits: 2, factor: 100},
    // OMR currency object
        OMR: { code: "OMR", minorUnits: 3, factor: 1000},
    // PAB currency object
        PAB: { code: "PAB", minorUnits: 2, factor: 100},
    // PEN currency object
        PEN: { code: "PEN", minorUnits: 2, factor: 100},
    // PGK currency object
        PGK: { code: "PGK", minorUnits: 2, factor: 100},
    // PHP currency object
        PHP: { code: "PHP", minorUnits: 2, factor: 100},
    // PKR currency object
        PKR: { code: "PKR", minorUnits: 2, factor: 100},
    // PLN currency object
        PLN: { code: "PLN", minorUnits: 2, factor: 100},
    // PYG currency object
        PYG: { code: "PYG", minorUnits: 0, factor: 1},
    // QAR currency object
        QAR: { code: "QAR", minorUnits: 2, factor: 100},
    // RON currency object
        RON: { code: "RON", minorUnits: 2, factor: 100},
    // RSD currency object
        RSD: { code: "RSD", minorUnits: 2, factor: 100},
    // RUB currency object
        RUB: { code: "RUB", minorUnits: 2, factor: 100},
    // RWF currency object
        RWF: { code: "RWF", minorUnits: 0, factor: 1},
    // SAR currency object
        SAR: { code: "SAR", minorUnits: 2, factor: 100},
    // SBD currency object
        SBD: { code: "SBD", minorUnits: 2, factor: 100},
    // SCR currency object
        SCR: { code: "SCR", minorUnits: 2, factor: 100},
    // SDG currency object
        SDG: { code: "SDG", minorUnits: 2, factor: 100},
    // SEK currency object
        SEK: { code: "SEK", minorUnits: 2, factor: 100},
    // SGD currency object
        SGD: { code: "SGD", minorUnits: 2, factor: 100},
    // SHP currency object
        SHP: { code: "SHP", minorUnits: 2, factor: 100},
    // SLL currency object
        SLL: { code: "SLL", minorUnits: 2, factor: 100},
    // SOS currency object
        SOS: { code: "SOS", minorUnits: 2, factor: 100},
    // SRD currency object
        SRD: { code: "SRD", minorUnits: 2, factor: 100},
    // SSP currency object
        SSP: { code: "SSP", minorUnits: 2, factor: 100},
    // STN currency object
        STN: { code: "STN", minorUnits: 2, factor: 100},
    // SVC currency object
        SVC: { code: "SVC", minorUnits: 2, factor: 100},
    // SYP currency object
        SYP: { code: "SYP", minorUnits: 2, factor: 100},
    // SZL currency object
        SZL: { code: "SZL", minorUnits: 2, factor: 100},
    // THB currency object
        THB: { code: "THB", minorUnits: 2, factor: 100},
    // TJS currency object
        TJS: { code: "TJS", minorUnits: 2, factor: 100},
    // TMT currency object
        TMT: { code: "TMT", minorUnits: 2, factor: 100},
    // TND currency object
        TND: { code: "TND", minorUnits: 3, factor: 1000},
    // TOP currency object
        TOP: { code: "TOP", minorUnits: 2, factor: 100},
    // TRY currency object
        TRY: { code: "TRY", minorUnits: 2, factor: 100},
    // TTD currency object
        TTD: { code: "TTD", minorUnits: 2, factor: 100},
    // TWD currency object
        TWD: { code: "TWD", minorUnits: 2, factor: 100},
    // TZS currency object
        TZS: { code: "TZS", minorUnits: 2, factor: 100},
    // UAH currency object
        UAH: { code: "UAH", minorUnits: 2, factor: 100},
    // UGX currency object
        UGX: { code: "UGX", minorUnits: 0, factor: 1},
    // USD currency object
        USD: { code: "USD", minorUnits: 2, factor: 100},
    // USN currency object
        USN: { code: "USN", minorUnits: 2, factor: 100},
    // UYI currency object
        UYI: { code: "UYI", minorUnits: 0, factor: 1},
    // UYU currency object
        UYU: { code: "UYU", minorUnits: 2, factor: 100},
    // UYW currency object
        UYW: { code: "UYW", minorUnits: 4, factor: 10000},
    // UZS currency object
        UZS: { code: "UZS", minorUnits: 2, factor: 100},
    // VES currency object
        VES: { code: "VES", minorUnits: 2, factor: 100},
    // VND currency object
        VND: { code: "VND", minorUnits: 0, factor: 1},
    // VUV currency object
        VUV: { code: "VUV", minorUnits: 0, factor: 1},
    // WST currency object
        WST: { code: "WST", minorUnits: 2, factor: 100},
    // XAF currency object
        XAF: { code: "XAF", minorUnits: 0, factor: 1},
    // XAG currency object
        XAG: { code: "XAG", minorUnits: 0, factor: 1},
    // XAU currency object
        XAU: { code: "XAU", minorUnits: 0, factor: 1},
    // XBA currency object
        XBA: { code: "XBA", minorUnits: 0, factor: 1},
    // XBB currency object
        XBB: { code: "XBB", minorUnits: 0, factor: 1},
    // XBC currency object
        XBC: { code: "XBC", minorUnits: 0, factor: 1},
    // XBD currency object
        XBD: { code: "XBD", minorUnits: 0, factor: 1},
    // XCD currency object
        XCD: { code: "XCD", minorUnits: 2, factor: 100},
    // XDR currency object
        XDR: { code: "XDR", minorUnits: 0, factor: 1},
    // XOF currency object
        XOF: { code: "XOF", minorUnits: 0, factor: 1},
    // XPD currency object
        XPD: { code: "XPD", minorUnits: 0, factor: 1},
    // XPF currency object
        XPF: { code: "XPF", minorUnits: 0, factor: 1},
    // XPT currency object
        XPT: { code: "XPT", minorUnits: 0, factor: 1},
    // XSU currency object
        XSU: { code: "XSU", minorUnits: 0, factor: 1},
    // XTS currency object
        XTS: { code: "XTS", minorUnits: 0, factor: 1},
    // XUA currency object
        XUA: { code: "XUA", minorUnits: 0, factor: 1},
    // XXX currency object
        XXX: { code: "XXX", minorUnits: 0, factor: 1},
    // YER currency object
        YER: { code: "YER", minorUnits: 2, factor: 100},
    // ZAR currency object
        ZAR: { code: "ZAR", minorUnits: 2, factor: 100},
    // ZMW currency object
        ZMW: { code: "ZMW", minorUnits: 2, factor: 100},
    // ZWL currency object
        ZWL: { code: "ZWL", minorUnits: 2, factor: 100}
    };